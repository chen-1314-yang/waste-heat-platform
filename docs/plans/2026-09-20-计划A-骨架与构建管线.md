# 计划 A：站点骨架与构建管线 · 实施计划

> **给执行者：** 这是四个子项目中的第一个。请用 `executing-plans` 逐任务执行，
> 每个任务结束都有可独立验收的产物。步骤用 `- [ ]` 勾选跟踪。

**目标：** 做出一个由 JSON 内容驱动、一条命令构建、断网可打开的站点外壳，
含十一个页签的导航与路由，其中四个新增页签填真实内容。

**架构：** 纯静态 HTML/CSS/JS，不用框架。`content/` 放内容，`src/` 放模板与代码，
`tools/build.py` 把两者合成出两种产物：多文件站点与单文件 HTML。
页签视图用普通脚本（非 ES module）挂在全局命名空间上，便于单文件拼接。

**技术栈：** Python 3.14（系统 `python`）+ pytest 9.1.1 + 原生 HTML/CSS/JS。
**不引入任何前端框架、不引 CDN、不下载字体。**

## 全局约束

- 站点根目录：`D:/Codex/2026-08-06/ni/时代杯项目材料/12_网站v4_实验版/`
- 不修改 `D:/Codex/2026-08-06/ni/时代杯项目材料/` 下 01–11 的任何文件
- 产物中不允许出现任何外部资源引用（`script src` / `link href` / `@import` / `url()`）
- 构建必须离线可完成
- 内容与代码分离：改文字/数字只动 `content/`，不动 `src/`
- 视觉令牌：`--bg:#F5F9FC; --ink:#16324F; --accent:#0E9F8A; --blue:#3D6FB4; --gold:#E8A33D; --red:#E05252`
- 每个任务结束必须 `git commit`

---

## 任务 1：内容文件与校验器

**文件：**
- 新建：`content/site.json`、`content/tabs.json`、`content/evidence.json`、
  `content/calibration.json`、`content/boundaries.json`、`content/changelog.json`
- 新建：`tools/validate_content.py`
- 测试：`tests/test_validate_content.py`

**接口：**
- 产出：`validate_content.ContentError`（异常类）
- 产出：`validate_content.load_and_validate(content_dir) -> dict`，
  返回六个键；任一条目不合规即抛 `ContentError`，消息含文件名与条目 id。

- [ ] **步骤 1：写失败的测试**

```python
# tests/test_validate_content.py
import json
import os
import sys

import pytest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import validate_content  # noqa: E402


def _write(tmp_path, name, payload):
    path = tmp_path / name
    path.write_text(json.dumps(payload, ensure_ascii=False), encoding="utf-8")


def _minimal(tmp_path):
    _write(tmp_path, "site.json", {"title": "T", "version": "1.0.0"})
    _write(tmp_path, "tabs.json", {"tabs": [
        {"id": "evidence", "title": "外部证据", "kind": "content"}]})
    _write(tmp_path, "evidence.json", {"items": []})
    _write(tmp_path, "calibration.json", {"status": "in-progress", "items": []})
    _write(tmp_path, "boundaries.json", {"items": []})
    _write(tmp_path, "changelog.json", {"entries": []})


def test_evidence_without_caveat_is_rejected(tmp_path):
    _minimal(tmp_path)
    _write(tmp_path, "evidence.json", {"items": [{
        "id": "x", "title": "标题", "value": "1", "scope": "范围",
        "source": "出处", "caveat": ""}]})
    with pytest.raises(validate_content.ContentError) as excinfo:
        validate_content.load_and_validate(str(tmp_path))
    assert "caveat" in str(excinfo.value)
    assert "x" in str(excinfo.value)


def test_calibration_bad_decision_is_rejected(tmp_path):
    _minimal(tmp_path)
    _write(tmp_path, "calibration.json", {"status": "in-progress", "items": [{
        "id": "c", "parameter": "P", "kernel_value": "1", "external": [],
        "finding": "f", "impact": "i", "decision": "maybe"}]})
    with pytest.raises(validate_content.ContentError) as excinfo:
        validate_content.load_and_validate(str(tmp_path))
    assert "decision" in str(excinfo.value)


def test_valid_content_passes(tmp_path):
    _minimal(tmp_path)
    _write(tmp_path, "evidence.json", {"items": [{
        "id": "x", "title": "标题", "value": "953 台", "scope": "范围",
        "source": "出处", "caveat": "无效率数据"}]})
    data = validate_content.load_and_validate(str(tmp_path))
    assert data["evidence"]["items"][0]["id"] == "x"
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_validate_content.py -v`
预期：FAIL，报 `ModuleNotFoundError: No module named 'validate_content'`

- [ ] **步骤 3：写最小实现**

```python
# tools/validate_content.py
"""内容文件校验。规则来自设计文档：没有适用边界的证据条目不允许入库。"""
import json
import os

DECISIONS = {"pending", "accepted", "rejected"}
KINDS = {"content", "placeholder", "tool"}

REQUIRED = {
    "evidence_item": ("id", "title", "value", "scope", "source", "caveat"),
    "calibration_item": ("id", "parameter", "kernel_value", "external",
                         "finding", "impact", "decision"),
    "boundary_item": ("item", "scope", "grade"),
    "changelog_entry": ("date", "version", "title", "changes",
                        "relative_to_prior"),
}


class ContentError(Exception):
    """内容不合规。"""


def _read(content_dir, name):
    path = os.path.join(content_dir, name)
    if not os.path.exists(path):
        raise ContentError(f"{name}: 文件不存在")
    with open(path, encoding="utf-8") as handle:
        return json.load(handle)


def _check_list(payload, key, kind, filename):
    items = payload.get(key)
    if not isinstance(items, list):
        raise ContentError(f"{filename}: 缺少列表字段 {key}")
    for index, item in enumerate(items):
        if not isinstance(item, dict):
            raise ContentError(f"{filename}[{index}]: 不是对象")
        ident = item.get("id") or item.get("item") or f"#{index}"
        for field in REQUIRED[kind]:
            value = item.get(field)
            if value is None or (isinstance(value, str) and not value.strip()):
                raise ContentError(f"{filename}[{ident}]: 字段 {field} 为空或缺失")
        if kind == "calibration_item" and item["decision"] not in DECISIONS:
            raise ContentError(
                f"{filename}[{ident}]: decision 只能是 {sorted(DECISIONS)}，"
                f"当前为 {item['decision']!r}")
        if kind == "changelog_entry" and not isinstance(item["changes"], list):
            raise ContentError(f"{filename}[{ident}]: changes 必须是列表")
    return items


def _check_tabs(payload):
    tabs = payload.get("tabs")
    if not isinstance(tabs, list) or not tabs:
        raise ContentError("tabs.json: 缺少非空 tabs 列表")
    for index, tab in enumerate(tabs):
        if not isinstance(tab, dict):
            raise ContentError(f"tabs.json[{index}]: 不是对象")
        for field in ("id", "title", "kind"):
            if not str(tab.get(field, "")).strip():
                raise ContentError(f"tabs.json[{index}]: 字段 {field} 缺失")
        if tab["kind"] not in KINDS:
            raise ContentError(
                f"tabs.json[{tab['id']}]: kind 只能是 {sorted(KINDS)}")
    return tabs


def load_and_validate(content_dir):
    site = _read(content_dir, "site.json")
    for field in ("title", "version"):
        if not str(site.get(field, "")).strip():
            raise ContentError(f"site.json: 字段 {field} 为空或缺失")

    tabs = _check_tabs(_read(content_dir, "tabs.json"))

    evidence = _read(content_dir, "evidence.json")
    _check_list(evidence, "items", "evidence_item", "evidence.json")

    calibration = _read(content_dir, "calibration.json")
    _check_list(calibration, "items", "calibration_item", "calibration.json")

    boundaries = _read(content_dir, "boundaries.json")
    _check_list(boundaries, "items", "boundary_item", "boundaries.json")

    changelog = _read(content_dir, "changelog.json")
    _check_list(changelog, "entries", "changelog_entry", "changelog.json")

    return {"site": site, "tabs": tabs, "evidence": evidence,
            "calibration": calibration, "boundaries": boundaries,
            "changelog": changelog}
```

- [ ] **步骤 4：写六个真实内容文件**

`content/tabs.json`（十一个页签；老站六个 + 自主学习实验室先标 `placeholder`，
四个新增内容页标 `content`。**顺序与设计文档第 3 节一致，不额外增加"概览"页**）：

```json
{"tabs": [
  {"id": "calc",        "title": "计算",       "kind": "placeholder"},
  {"id": "realtime",    "title": "实时模拟",   "kind": "placeholder"},
  {"id": "pareto",      "title": "帕累托前沿", "kind": "placeholder"},
  {"id": "lca",         "title": "LCA",        "kind": "placeholder"},
  {"id": "conds",       "title": "工况库",     "kind": "placeholder"},
  {"id": "method",      "title": "方法说明",   "kind": "placeholder"},
  {"id": "evidence",    "title": "外部证据",   "kind": "content"},
  {"id": "calibration", "title": "校准工作台", "kind": "content"},
  {"id": "boundaries",  "title": "边界与口径", "kind": "content"},
  {"id": "changelog",   "title": "演进记录",   "kind": "content"},
  {"id": "learning",    "title": "自主学习实验室", "kind": "placeholder"}
]}
```

`content/site.json`：

```json
{"title": "工业余热回收 · 智能决策平台（新版）", "version": "1.0.0",
 "team": "（团队名）", "members": "（成员名单占位）",
 "banner": "本页为独立新版站点，与已上线的旧版互不影响"}
```

`content/evidence.json`（本计划先收 4 条，其余在计划 C 补全；**每条必须有 caveat**）：

```json
{"items": [
  {"id": "orc-world-map", "title": "全球 ORC 装机数据库",
   "value": "953 台 / 3512.7 MW",
   "scope": "工业余热回收 331 台；中国境内 31 台；水泥 12 台；冶金 33 台",
   "source": "ORC World Map（Tartiere），厂商官网+新闻+论文交叉核对",
   "url": "https://github.com/tartieret/ORC-World-Map-SPA",
   "caveat": "无热源温度、无效率；953 台中 741 台工质缺失；含年份脏值"},
  {"id": "eu28-waste-heat", "title": "EU28 工业过程与余热数据（TNO）",
   "value": "267 行，含热量（TJ/a）与温度（℃）",
   "scope": "按工业产品/工艺分列，附余热侧两个物流",
   "source": "Mendeley Data 10.17632/gyxjmvzbx8",
   "url": "https://data.mendeley.com/datasets/gyxjmvzbx8",
   "caveat": "工艺级汇总，非单一工厂实测；温度为工艺代表值"},
  {"id": "gb-standards", "title": "中国国家标准（余热相关）",
   "value": "10 项现行",
   "scope": "含 GB/T 37819-2019 低温余热双循环发电装置、GB/T 40286-2021 性能测试方法",
   "source": "国家标准全文公开系统检索",
   "url": "https://openstd.samr.gov.cn/",
   "caveat": "标准给出的是测试与设计方法，不含具体工程数据"},
  {"id": "pypsa-orc-cost", "title": "PyPSA 技术造价库 · ORC",
   "value": "1376 EUR/kW_el；寿命 30 年；FOM 2%/年",
   "scope": "欧洲能源系统建模通用造价库",
   "source": "Tartiere & Astolfi 2017《A world overview of the organic Rankine cycle market》",
   "url": "https://github.com/PyPSA/technology-data",
   "caveat": "原文注明低装机量使估算不确定；为欧洲口径"}
]}
```

`content/calibration.json`（先 1 条，`decision` 必须为 `pending`）：

```json
{"status": "in-progress", "items": [
  {"id": "orc-unit-cost", "parameter": "ORC 单位造价",
   "kernel_value": "23800 元/kW（91.5 kW 小机组固定单价）",
   "external": [
     {"source": "中国工程口径", "value": "6400-8700 元/kW", "scope": "蒸汽路径 6-9 MW"},
     {"source": "HeatScout 关联式", "value": "C = 3000 x Q^0.75 EUR（含安装 x1.6）"},
     {"source": "PyPSA 技术造价库", "value": "1376 EUR/kW_el"}],
   "finding": "内核用 91.5 kW 小机组固定单价代表全规模（100 kW-20 MW），属量纲不匹配",
   "impact": "若切换口径，ORC 隐含回收期由 5.6 年降至 1.2-2.9 年，可能翻转推荐结论",
   "decision": "pending"}
]}
```

`content/boundaries.json`（5 条，字段为 `item` / `scope` / `grade`）：

```json
{"items": [
  {"item": "ORC 减碳量", "scope": "仿真净功率 x 运行小时 x 电网因子",
   "grade": "推算，非实测"},
  {"item": "供热减碳", "scope": "替代天然气估算", "grade": "演示估算"},
  {"item": "成本 / 回收期", "scope": "代理模型", "grade": "示意，非报价"},
  {"item": "工况库非公开案例", "scope": "工程示例", "grade": "示意"},
  {"item": "TOPSIS 指标", "scope": "典型演示值", "grade": "待替换"}
]}
```

`content/changelog.json`：

```json
{"entries": [
  {"date": "2026-09-20", "version": "1.0.0", "title": "新版站点起步",
   "changes": ["建立内容驱动骨架", "新增外部证据层", "新增校准工作台",
               "新增边界与口径页", "建立演进记录"],
   "relative_to_prior": "首版，无前序版本；交付版口径尚未并入本版"}
]}
```

- [ ] **步骤 5：运行测试确认通过**

运行：`python -m pytest tests/test_validate_content.py -v`
预期：3 passed

- [ ] **步骤 6：用真实内容跑一次校验**

运行：
`python -c "import sys; sys.path.insert(0,'tools'); import validate_content; d=validate_content.load_and_validate('content'); print('页签', len(d['tabs']), '证据', len(d['evidence']['items']), '边界', len(d['boundaries']['items']))"`
预期：`页签 11 证据 4 边界 5`

- [ ] **步骤 7：提交**

```bash
git add content tools/validate_content.py tests/test_validate_content.py
git commit -m "feat(content): 内容模型与校验器；证据条目强制带适用边界"
```

---

## 任务 2：构建脚本与模板

**文件：** 新建 `tools/build.py`、`src/index.html`、`tests/test_build.py`

**接口：**
- 消费：`validate_content.load_and_validate(content_dir)`
- 产出：`build.build(content_dir, src_dir, out_dir) -> dict`，
  返回 `{"site_dir": …, "single_file": …}`

- [ ] **步骤 1：写失败的测试**

```python
# tests/test_build.py
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import build  # noqa: E402

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))


def _run(tmp_path):
    return build.build(os.path.join(ROOT, "content"),
                       os.path.join(ROOT, "src"), str(tmp_path))


def test_build_produces_two_artifacts(tmp_path):
    result = _run(tmp_path)
    assert os.path.isfile(os.path.join(result["site_dir"], "index.html"))
    assert os.path.isfile(result["single_file"])


def test_site_index_carries_content(tmp_path):
    result = _run(tmp_path)
    html = open(os.path.join(result["site_dir"], "index.html"), encoding="utf-8").read()
    assert "外部证据" in html
    assert "953 台" in html


def test_single_file_is_self_contained(tmp_path):
    result = _run(tmp_path)
    html = open(result["single_file"], encoding="utf-8").read()
    assert "<style>" in html
    assert 'src="./assets' not in html
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_build.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 'build'`

- [ ] **步骤 3：写模板 `src/index.html`**

```html
<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>__SITE_TITLE__</title>
<!--__STYLE__-->
</head>
<body>
<header class="topbar">
  <div class="brand">__SITE_TITLE__</div>
  <div class="badge">__SITE_BANNER__</div>
</header>
<nav class="tabs" id="tabs"></nav>
<main id="view"></main>
<script>window.__CONTENT__ = __CONTENT_JSON__;</script>
<!--__SCRIPT__-->
</body>
</html>
```

- [ ] **步骤 4：写 `tools/build.py`**

```python
"""把 content/ 与 src/ 合成两种产物：多文件站点 + 单文件 HTML。"""
import json
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import validate_content  # noqa: E402

SINGLE_NAME = "工业余热回收_新版_单文件.html"
STYLE_FILES = ["tokens.css", "base.css", "components.css"]
SCRIPT_FILES = ["views/placeholder.js", "views/evidence.js", "views/calibration.js",
                "views/boundaries.js", "views/changelog.js", "app.js"]


def _read(path):
    with open(path, encoding="utf-8") as handle:
        return handle.read()


def build(content_dir, src_dir, out_dir):
    data = validate_content.load_and_validate(content_dir)
    os.makedirs(out_dir, exist_ok=True)

    css = "\n".join(_read(os.path.join(src_dir, "styles", name))
                    for name in STYLE_FILES)
    js = "\n".join(_read(os.path.join(src_dir, name)) for name in SCRIPT_FILES)
    template = _read(os.path.join(src_dir, "index.html"))

    payload = json.dumps(data, ensure_ascii=False)
    title = data["site"]["title"]
    banner = data["site"].get("banner", "")

    common = (template
              .replace("__SITE_TITLE__", title)
              .replace("__SITE_BANNER__", banner)
              .replace("__CONTENT_JSON__", payload))

    multi = (common
             .replace("<!--__STYLE__-->",
                      '<link rel="stylesheet" href="./assets/style.css">')
             .replace("<!--__SCRIPT__-->",
                      '<script src="./assets/app.js"></script>'))

    single = (common
              .replace("<!--__STYLE__-->", "<style>\n" + css + "\n</style>")
              .replace("<!--__SCRIPT__-->", "<script>\n" + js + "\n</script>"))

    site_dir = os.path.join(out_dir, "site")
    os.makedirs(os.path.join(site_dir, "assets"), exist_ok=True)
    with open(os.path.join(site_dir, "index.html"), "w", encoding="utf-8") as handle:
        handle.write(multi)
    with open(os.path.join(site_dir, "assets", "style.css"), "w",
              encoding="utf-8") as handle:
        handle.write(css)
    with open(os.path.join(site_dir, "assets", "app.js"), "w",
              encoding="utf-8") as handle:
        handle.write(js)

    single_path = os.path.join(out_dir, SINGLE_NAME)
    with open(single_path, "w", encoding="utf-8") as handle:
        handle.write(single)

    return {"site_dir": site_dir, "single_file": single_path}


if __name__ == "__main__":
    root = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))
    result = build(os.path.join(root, "content"), os.path.join(root, "src"),
                   os.path.join(root, "dist"))
    for key, value in result.items():
        size = os.path.getsize(value) / 1024 if os.path.isfile(value) else 0
        print(f"{key}: {value}  {size:.1f} KB")
```

- [ ] **步骤 5：创建样式与脚本的骨架，让构建先跑通**

`src/styles/tokens.css`：

```css
:root{
  --bg:#F5F9FC; --card:#FFFFFF; --line:#DCE7F0;
  --ink:#16324F; --muted:#5E7387; --soft:#8AA0B4;
  --accent:#0E9F8A; --accent-soft:#EAF6F3; --accent-line:#BEE5DC;
  --blue:#3D6FB4; --gold:#E8A33D; --red:#E05252;
  --radius:12px; --gap:16px;
  --font-cn:"Microsoft YaHei","PingFang SC","Segoe UI",sans-serif;
}
```

`src/styles/base.css` 与 `src/styles/components.css` 先只写最少内容
（如 `*{box-sizing:border-box}`），任务 3 补全。

`src/views/` 下先各建一个文件，把视图函数挂在 `window.VIEWS` 上；
`src/views/placeholder.js` 内容为：

```javascript
window.VIEWS = window.VIEWS || {};
window.VIEWS.placeholder = function (content) {
  return '<section class="card"><h2>本页尚未迁移到新版</h2>' +
         '<p>该页签的原有能力将在后续计划中迁移过来。</p></section>';
};
```

其余四个视图文件先各写最小实现（返回含标题的 `section`），任务 5 补全。

`src/app.js` 先写最小版本（任务 4 补全）。

- [ ] **步骤 6：运行测试确认通过**

运行：`python -m pytest tests/test_build.py -v`
预期：3 passed

- [ ] **步骤 7：提交**

```bash
git add tools/build.py src tests/test_build.py
git commit -m "feat(build): 构建管线产出多文件站点与单文件 HTML"
```

---

## 任务 3：样式系统

**文件：** 修改 `src/styles/base.css`、`src/styles/components.css`；新建 `tests/test_styles.py`

**接口：** 消费 `build.build(...)`；产出可通过 CSS 变量换肤的完整样式。

- [ ] **步骤 1：写失败的测试**

```python
# tests/test_styles.py
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import build  # noqa: E402

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))
TOKENS = ["--bg", "--ink", "--accent", "--blue", "--gold", "--red"]


def _css(tmp_path):
    result = build.build(os.path.join(ROOT, "content"),
                         os.path.join(ROOT, "src"), str(tmp_path))
    return open(os.path.join(result["site_dir"], "assets", "style.css"),
                encoding="utf-8").read()


def test_tokens_present(tmp_path):
    css = _css(tmp_path)
    for token in TOKENS:
        assert token in css, f"缺少样式令牌 {token}"


def test_no_remote_font_or_import(tmp_path):
    css = _css(tmp_path)
    assert "@import" not in css
    assert "url(http" not in css


def test_component_selectors_exist(tmp_path):
    css = _css(tmp_path)
    for selector in (".tabs", ".tab", ".card", ".stat", ".chip"):
        assert selector in css, f"缺少选择器 {selector}"
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_styles.py -v`
预期：FAIL，`.tabs` / `.card` / `.stat` / `.chip` 缺失

- [ ] **步骤 3：写 `base.css`**

必须包含：

```css
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{background:var(--bg);color:var(--ink);font-family:var(--font-cn);
     font-size:15px;line-height:1.7}
.topbar{position:sticky;top:0;z-index:10;display:flex;flex-wrap:wrap;
        gap:12px;align-items:center;justify-content:space-between;
        padding:12px 20px;background:var(--card);border-bottom:1px solid var(--line)}
.brand{font-size:17px;font-weight:700}
.badge{font-size:12px;color:var(--muted)}
main{max-width:1120px;margin:0 auto;padding:20px}
h2{font-size:19px;margin:0 0 12px}
h3{font-size:16px;margin:18px 0 8px}
a{color:var(--blue)}
table{width:100%;border-collapse:collapse;font-size:14px}
th,td{border-bottom:1px solid var(--line);padding:8px 10px;text-align:left;
      vertical-align:top}
```

- [ ] **步骤 4：写 `components.css`**

必须包含 `.tabs`（横向可滚动）、`.tab` 与 `.tab.active`、
`.card`（白底 + `--line` 边框 + `--radius` 圆角）、
`.stat`（数字卡，数字用 `--accent` 且字号明显大于标签）、
`.chip`（等级标签，四种色调分别用 `--accent`/`--blue`/`--gold`/`--red` 的浅色底）、
`.grid`（卡片网格，`repeat(auto-fill,minmax(280px,1fr))`）。

**要求：** 数据色不超过 4 色；不用渐变背景；`@media (max-width:600px)` 下
表格改为纵向堆叠，避免横向溢出。

- [ ] **步骤 5：运行测试确认通过**

运行：`python -m pytest tests/test_styles.py -v`
预期：3 passed

- [ ] **步骤 6：提交**

```bash
git add src/styles tests/test_styles.py
git commit -m "feat(style): 令牌化样式系统；数据色限四色，无外部字体"
```

---

## 任务 4：页签外壳与路由

**文件：** 修改 `src/app.js`；新建 `tests/test_shell.py`

**接口：**
- 产出：`window.VIEWS`（对象，键为页签 id，值为 `(content) => string`）
- 产出：`window.renderTab(tabId)`、`window.currentTab()`

- [ ] **步骤 1：写失败的测试**

```python
# tests/test_shell.py
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import build  # noqa: E402

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))
TAB_IDS = ["calc", "realtime", "pareto", "lca", "conds", "method",
           "evidence", "calibration", "boundaries", "changelog", "learning"]


def _single(tmp_path):
    result = build.build(os.path.join(ROOT, "content"),
                         os.path.join(ROOT, "src"), str(tmp_path))
    return open(result["single_file"], encoding="utf-8").read()


def test_all_eleven_tabs_registered(tmp_path):
    html = _single(tmp_path)
    for tab_id in TAB_IDS:
        assert f'"{tab_id}"' in html, f"页签 {tab_id} 未注册"


def test_router_functions_exist(tmp_path):
    html = _single(tmp_path)
    assert "window.renderTab" in html
    assert "window.VIEWS" in html


def test_tab_titles_come_from_content(tmp_path):
    html = _single(tmp_path)
    assert "外部证据" in html and "校准工作台" in html
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_shell.py -v`
预期：FAIL，`window.renderTab` 未定义

- [ ] **步骤 3：写 `app.js`**

```javascript
window.VIEWS = window.VIEWS || {};

function renderTabs() {
  var nav = document.getElementById('tabs');
  var tabs = window.__CONTENT__.tabs;
  var out = '';
  for (var i = 0; i < tabs.length; i++) {
    out += '<button class="tab" data-tab="' + tabs[i].id + '">' +
           tabs[i].title + '</button>';
  }
  nav.innerHTML = out;
}

window.renderTab = function (tabId) {
  var content = window.__CONTENT__;
  var view = window.VIEWS[tabId];
  if (!view) { view = window.VIEWS.placeholder; }
  document.getElementById('view').innerHTML = view(content);
  var buttons = document.querySelectorAll('.tab');
  for (var i = 0; i < buttons.length; i++) {
    if (buttons[i].dataset.tab === tabId) {
      buttons[i].classList.add('active');
    } else {
      buttons[i].classList.remove('active');
    }
  }
  if (location.hash !== '#' + tabId) { location.hash = tabId; }
};

window.currentTab = function () {
  var id = (location.hash || '').replace('#', '');
  var tabs = window.__CONTENT__.tabs;
  for (var i = 0; i < tabs.length; i++) {
    if (tabs[i].id === id) { return id; }
  }
  return tabs[0].id;
};

document.addEventListener('DOMContentLoaded', function () {
  renderTabs();
  document.getElementById('tabs').addEventListener('click', function (event) {
    var id = event.target.dataset ? event.target.dataset.tab : null;
    if (id) { window.renderTab(id); }
  });
  window.renderTab(window.currentTab());
});
```

- [ ] **步骤 4：运行测试确认通过**

运行：`python -m pytest tests/test_shell.py -v`
预期：3 passed

- [ ] **步骤 5：人工验证路由**

运行：`python tools/build.py`，浏览器打开
`dist/工业余热回收_新版_单文件.html`，逐个点击十一个页签。
预期：高亮正确切换；地址栏 hash 变化；刷新后停留在同一页签。

- [ ] **步骤 6：提交**

```bash
git add src/app.js tests/test_shell.py
git commit -m "feat(shell): 十一页签外壳与 hash 路由"
```

---

## 任务 5：四个内容页签的渲染

**文件：** 修改 `src/views/evidence.js`、`calibration.js`、`boundaries.js`、`changelog.js`；
新建 `tests/test_views.py`

**接口：** 每个视图为 `(content) => string`，注册到 `window.VIEWS[<页签 id>]`。

- [ ] **步骤 1：写失败的测试**

```python
# tests/test_views.py
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import build  # noqa: E402

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))


def _single(tmp_path):
    result = build.build(os.path.join(ROOT, "content"),
                         os.path.join(ROOT, "src"), str(tmp_path))
    return open(result["single_file"], encoding="utf-8").read()


def test_evidence_view_registered(tmp_path):
    html = _single(tmp_path)
    assert "VIEWS.evidence" in html or 'VIEWS["evidence"]' in html


def test_calibration_shows_pending_badge(tmp_path):
    html = _single(tmp_path)
    assert "待定" in html, "校准页必须把未拍板项显示为待定"


def test_boundaries_view_registered(tmp_path):
    html = _single(tmp_path)
    assert "VIEWS.boundaries" in html or 'VIEWS["boundaries"]' in html


def test_changelog_view_registered(tmp_path):
    html = _single(tmp_path)
    assert "VIEWS.changelog" in html or 'VIEWS["changelog"]' in html
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_views.py -v`
预期：`test_calibration_shows_pending_badge` 失败

- [ ] **步骤 3：实现四个视图**

要求：
- **evidence**：`.grid` 卡片；每条显示 title / value / scope / source / **caveat**；
  caveat 用 `.chip` 醒目标出"适用边界"；有 url 的渲染成
  `<a href="…" target="_blank" rel="noopener">`。
- **calibration**：表格，列 = 参数 / 内核现值 / 外部口径 / 差异 / 影响 / 状态；
  `pending` 显示黄色「待定」、`accepted` 绿色「已采纳」、`rejected` 灰色「未采纳」；
  **页顶固定一行提示：待定项不参与演示数值计算。**
- **boundaries**：三列表格（项目 / 口径 / 等级），等级用 `.chip` 区分颜色。
- **changelog**：时间线；每条显示 date / version / title / changes / relative_to_prior。

四个视图都要做空数据兜底（无条目时显示"暂无条目"），不得抛异常；
对外部字段一律用 `文本转义` 后再插入 HTML（防止内容里的 `<` 破坏页面）。

- [ ] **步骤 4：运行测试确认通过**

运行：`python -m pytest tests/test_views.py -v`
预期：4 passed

- [ ] **步骤 5：运行全量测试**

运行：`python -m pytest -v`
预期：全部通过

- [ ] **步骤 6：人工检查**

重新构建，浏览器打开单文件版；检查四个页签内容完整、排版不重叠；
窗口缩到 375px 宽时不横向溢出。

- [ ] **步骤 7：提交**

```bash
git add src/views tests/test_views.py
git commit -m "feat(views): 外部证据/校准/边界/演进四页；校准待定项醒目标注"
```

---

## 任务 6：离线与体积校验

**文件：** 新建 `tests/test_offline.py`

- [ ] **步骤 1：写测试**

```python
# tests/test_offline.py
import os
import re
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "tools"))
import build  # noqa: E402

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))


def _build(tmp_path):
    return build.build(os.path.join(ROOT, "content"),
                       os.path.join(ROOT, "src"), str(tmp_path))


def test_no_external_subresources(tmp_path):
    html = open(_build(tmp_path)["single_file"], encoding="utf-8").read()
    for pattern in (r'<script[^>]+src="https?://', r'<link[^>]+href="https?://',
                    r"@import\s+url\(", r"url\(https?://"):
        assert not re.search(pattern, html, re.I), f"发现外部资源：{pattern}"


def test_single_file_self_contained(tmp_path):
    html = open(_build(tmp_path)["single_file"], encoding="utf-8").read()
    assert "<style>" in html
    assert html.count("<script") >= 2
    assert "window.__CONTENT__" in html


def test_single_file_size_budget(tmp_path):
    path = _build(tmp_path)["single_file"]
    size_kb = os.path.getsize(path) / 1024
    assert size_kb < 400, f"单文件 {size_kb:.0f} KB 超出预算 400 KB"
```

- [ ] **步骤 2：运行测试**

运行：`python -m pytest tests/test_offline.py -v`
预期：3 passed（若失败，按提示修掉外部资源引用）

- [ ] **步骤 3：真断网验证**

断开网络，双击 `dist/工业余热回收_新版_单文件.html`。
预期：十一个页签全部可用，四个内容页签内容完整，样式正常。

- [ ] **步骤 4：运行全量回归**

运行：`python -m pytest -v`
预期：全部通过

- [ ] **步骤 5：提交并打标签**

```bash
git add tests/test_offline.py
git commit -m "test: 离线自足性与体积预算校验"
git tag plan-a-done
```

---

## 计划 A 完成后的状态

- 一条命令构建：`python tools/build.py`，产出多文件站点与单文件 HTML；
- 十一个页签可导航，四个新增页签有真实内容，七个老页签显示迁移占位；
- 全部校验自动化，`python -m pytest -v` 一键回归；
- 为后续三份计划留好接口：
  - **计划 B**：迁移老站六个功能页签（决策内核 + 演示数据）
  - **计划 C**：补全外部证据层内容（953 台装机库、EU28、国标、造价三口径等）
  - **计划 D**：自主学习实验室（五道门控 + 版本 + 回滚）
